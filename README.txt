To run the program in a console, first make sure all .c, .h, and intersections.txt and trains,txt files are in the same(current) directory. Then use the command "gcc main.c -o main" to compile the main file.
Then you use the command "./main" to run the program. It will take intersections.txt and trains.txt as input.
